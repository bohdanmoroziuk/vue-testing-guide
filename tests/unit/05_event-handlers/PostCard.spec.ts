// import { mount } from '@vue/test-utils';

// import PostCard from '../../../src/components/05_event-handlers/PostCard.vue';

describe('PostCard', () => {
  const mockPost = {
    id: 'dadfasfsa',
    title: 'Post Title',
    content: 'Post Content',
  };

  it.todo('emits a delete event on button click');
});
