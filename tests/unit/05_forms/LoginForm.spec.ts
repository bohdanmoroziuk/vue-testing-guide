// import { mount } from '@vue/test-utils';

// import LoginFrom from '../../../src/components/05_forms/LoginForm.vue';

describe('LoginForm', () => {
  describe('on submit button click', () => {
    describe('when not all fields are filled', () => {
      it.todo('renders an error message');

      it.todo('does not submit the form');
    });

    describe('when all fields are filled', () => {
      it.todo('submits the form on button click');
    });
  });
});
